# Database

Planned tables include profiles, roles, permissions, students, guardians, applications, documents, reviews, payments, notification logs, selection campaigns, selection runs, results, audit logs, and system settings.

All sensitive tables must use RLS, UUID primary keys, timestamps, foreign keys, indexes, and immutable audit records where required.
