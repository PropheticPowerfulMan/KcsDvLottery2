# Security

Security requirements include RBAC, RLS, signed document URLs, server-side validation, webhook signature verification, rate limiting, audit logs, secret management, and strong admin authentication.
